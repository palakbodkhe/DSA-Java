class Solution {
    public int findMin(int n) {
        // code here
        int coins[] = {10, 5, 2, 1};
        int count = 0;

        for (int coin : coins) {
            while (n >= coin) {
                n -= coin;
                count++;
            }
        }
        return count;
    }
}
